rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Temporary: Allow all read/write until Jan 31, 2026
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 1, 31);
    }
  }
}